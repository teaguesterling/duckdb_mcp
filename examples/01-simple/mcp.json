{
  "mcpServers": {
    "duckdb-mcp-test": {
      "command": "./build/release/duckdb",
      "args": [
        "-s",
        "LOAD duckdb_mcp",
        "-s",
        "SELECT mcp_server_start('stdio')"
      ]
    }
  }
}
